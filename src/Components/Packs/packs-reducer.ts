



type ActionsType = {}


export type PacksInitialStateType = {}

const initialState: PacksInitialStateType = {}

export const packsReducer = (state = initialState, action: ActionsType): PacksInitialStateType => {
    switch (action) {

        default:
            return state
    }
}